[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-research-os"
version = "0.1.0"
description = "Deterministic, simulation-only quantitative research tools"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
quant-research = "quant_research.cli:main"
quant-study = "quant_research.study:main"
quant-dashboard = "quant_research.dashboard:main"
quant-benchmark = "quant_research.benchmark:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]
